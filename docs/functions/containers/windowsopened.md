# windowsopened(...)

Returns amount of windows opened or true if backpacks names written. Accept backpacks IDs and Names

{% code lineNumbers="true" %}
```lua
if windowsopened("Moon Backpack","Red Backpack") then
  gotolabel("ToExp")
end
```
{% endcode %}
